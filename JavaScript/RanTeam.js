function runList() 
{
    const groupText = document.getElementById('group').value;
    const spliter = groupText.split(",")

}
function runInt()
{
    const amountInt = document.getElementById('amount').value;
    const groupAmount = parseInt(amountInt);
    let timesHell = 0
    const newInt = document.createElement('amount')

    while (timesHell < groupAmount)
    {
        newInt;
        timesHell++;
    }
}
